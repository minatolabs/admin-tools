# Tenant Operations

## Example Command

To run the M365 Hybrid Admin Tool, use the following command:

```powershell
powershell.exe -NoProfile -ExecutionPolicy Bypass -File "C:\path\to\M365-Hybrid-Admin-Tool.ps1"
```

Make sure to adjust the path to match the location of your script.

# Other existing content...